import { OrderDescPipePipe } from './order-desc-pipe.pipe';

describe('OrderDescPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDescPipePipe();
    expect(pipe).toBeTruthy();
  });
});
